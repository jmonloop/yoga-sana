<p class="pendiente">
  <strong>PENDIENTE DE TEXTO — Natalia:</strong> <slot /> Sustituir antes de publicar.
</p>

<style>
  .pendiente {
    padding: 1rem 1.25rem;
    border: 2px dashed var(--rosa-text);
    border-radius: var(--radius-card);
    color: var(--rosa-text);
    font-weight: 500;
  }
</style>
