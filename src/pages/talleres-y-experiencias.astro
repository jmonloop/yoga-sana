---
import Base from '../layouts/Base.astro';
import Banda from '../components/Banda.astro';
import Section from '../components/Section.astro';
import Foto from '../components/Foto.astro';
import Tarjetas from '../components/Tarjetas.astro';
import WaButton from '../components/WaButton.astro';
import { actividadesDe } from '../components/actividad';
import { CTAS } from '../data/site';
import { SNAPSHOT } from '../data/snapshot';
import { enlacesPara, paginasDeTalleres } from '../data/talleres';

const talleres = actividadesDe(SNAPSHOT.actividades, 'taller');
const enlaces = enlacesPara(talleres, paginasDeTalleres(talleres));
---

<Base
  title="Talleres de breathwork y experiencias en Catarroja (València) — Yoga Sana"
  description="Breathwork, rebirthing consciente, constelaciones familiares, baño de cuencos, gimnasia pasiva y Sanergía: talleres y sesiones individuales en Catarroja, cerca de València."
>
  <Section eyebrow="Date un espacio">
    <div class="columna">
      <h1>Talleres de breathwork y experiencias en Catarroja</h1>
      <p class="lead">
        Breathwork, rebirthing, constelaciones familiares, baño de cuencos y más: encuentros
        puntuales para respirar hondo, mirar hacia dentro y soltar lo que pesa, en Catarroja
        (València) y algunos también online.
      </p>
      <WaButton {...CTAS.empezar} />
    </div>
  </Section>

  {
    talleres.length > 0 && (
      <Section title="Los talleres" muted animar>
        <p class="intro">
          Cada propuesta tiene su propio ritmo: algunas se repiten una vez al mes, otras son
          un grupo fijo cada quince días y otras se convocan puntualmente. Por eso no están
          en el horario semanal. Las que tienen una frecuencia fija lo dicen en su tarjeta;
          para el resto, escríbeme y te aviso de la próxima. En cada tarjeta tienes un enlace
          para leer más sobre el taller.
        </p>
        <Tarjetas actividades={talleres} enlaces={enlaces} />
      </Section>
    )
  }

  <Section title="Sesiones individuales" animar>
    <Banda>
      <p>
        No todo pasa en grupo. La gestión emocional y la Sanergía también se hacen en sesión
        individual, a solas y a tu ritmo.
      </p>
      <p>
        Escríbeme, me cuentas qué necesitas y vemos cuándo nos viene bien y por dónde
        empezar.
      </p>
      <WaButton {...CTAS.sesionIndividual} />
      <Foto
        slot="foto"
        nombre="centro-camilla"
        sizes="(min-width: 70rem) 30rem, (min-width: 60rem) 26rem, 92vw"
        alt="Sala de tratamientos del centro: la camilla preparada bajo la estantería con los bolsters."
      />
    </Banda>
  </Section>

  <Section title="¿Te apetece probar?" muted animar>
    <p class="intro">
      Escríbeme por WhatsApp y te cuento qué hay en marcha, cuándo es lo próximo y qué te
      puede encajar mejor.
    </p>
    <WaButton {...CTAS.empezar} />
  </Section>
</Base>
